import './style.css'

const app = document.createElement('div')
app.id = 'app'
app.innerHTML = `<h1>Hej Vite + TypeScript</h1>`

document.body.appendChild(app)

const list = document.createElement('ul')